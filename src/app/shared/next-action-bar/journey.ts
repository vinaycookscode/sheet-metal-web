/**
 * The order-to-cash journey shown in the guided stepper across the sales screens,
 * so a user always sees where the current record sits in the overall flow.
 * Index: 0 Inquiry · 1 Quote · 2 Sales Order · 3 Production · 4 Dispatch.
 */
export const JOURNEY_STAGES = ['Inquiry', 'Quote', 'Sales Order', 'Production', 'Dispatch'];
